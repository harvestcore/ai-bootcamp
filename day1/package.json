{
  "name": "draw-and-guess",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer draw-and-guess game (skribbl.io style)",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "node scripts/build.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
